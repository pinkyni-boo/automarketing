import Link from 'next/link';import Image from 'next/image';import { ArrowRight, Clock } from 'lucide-react';
export default function PostCard({post}:any){return <article className="post-card"><Link href={`/blog/${post.slug}`} className="post-image"><Image src={post.image} alt={post.title} fill sizes="(max-width:768px) 100vw, 33vw"/><span className="category-pill">{post.category}</span></Link><div className="post-body"><div className="post-meta"><span>{post.date}</span><span><Clock size={14}/>{post.readTime}</span></div><h3><Link href={`/blog/${post.slug}`}>{post.title}</Link></h3><p>{post.excerpt}</p><Link className="read-more" href={`/blog/${post.slug}`}>Đọc thêm <ArrowRight size={16}/></Link></div></article>}
